using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace ControlsLIB
{
    public enum MoveInDirection { DefaultAxis, HMD, LeftHand, RightHand };
    public enum GestureDirection { None, Up, Down, Left, Right, Forward, Backward };
    public enum HandAction { Idle, ForceGrabbed, Grabbed, ForceAbility}

    public class ControlsLIB : MonoBehaviour
    {
     
    }
}
